<table class='table table-hover table-striped'>
  <% @votes.each do |text, count| %>
    <tr>
      <th><%= text.split.map(&:capitalize).join(' ') %></th>
      <td><%= count || 0 %>
    </tr>
  <% end %>
</table>
<p><a href='/'>Cast more votes!</a></p>
